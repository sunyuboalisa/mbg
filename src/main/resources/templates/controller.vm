package ${controllerPackageName};

import ${modelPackageName}.${modelName};
import ${servicePackageName}.${modelName}Service;
import com.alisa.util.Result;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.web.bind.annotation.*;

import java.util.List;

@RestController
@RequestMapping("/${modelNameLower}")
public class ${modelName}Controller {

    @Autowired
    private ${modelName}Service ${modelNameLower}Service;

    @GetMapping("/getAll")
    public Result<List<${modelName}>> getAll() {
        var data = ${modelNameLower}Service.getAll();
        return new Result<>(data);
    }

    @GetMapping("/{id}")
    public Result<${modelName}> getById(@PathVariable ${pkType} id) {
        var data = ${modelNameLower}Service.getById(id);
        return new Result<>(data);
    }

    @PostMapping("/upsert")
<<<<<<< HEAD
    public Result<Integer> upsert(@RequestBody ${modelName} ${modelNameLower}) {
        var count = ${modelNameLower}Service.upsert(${modelNameLower});
        return new Result<Integer>(count);
    }

    @PostMapping("/batch-upsert")
    public Result<Integer> batchUpsert(@RequestBody List<${modelName}> list) {
        var count = ${modelNameLower}Service.batchUpsert(list);
        return new Result<>(count);
    }

    @DeleteMapping("/batch-delete")
    public Result<Integer> batchDelete(@RequestBody List<${pkType}> ids) {
        var count = ${modelNameLower}Service.batchDelete(ids);
        return new Result<>(count);
=======
    public int upsert(@RequestBody ${modelName} ${modelNameLower}) {
        return ${modelNameLower}Service.upsert(${modelNameLower});
    }

    @PostMapping("/batch-upsert")
    public int batchUpsert(@RequestBody List<${modelName}> list) {
        return ${modelNameLower}Service.batchUpsert(list);
    }

    @DeleteMapping("/batch-delete")
    public int batchDelete(@RequestBody List<${pkType}> ids) {
        return ${modelNameLower}Service.batchDelete(ids);
>>>>>>> 05b5a956489efc547b2519e1bec0ec8ff3bf6ded
    }
}